direction: right

cmdguard v3.0.0 — Architecture: current state + 2 optional deltas

core: {
  shape: package
  style.fill: "#1e1b4b"
  style.font-color: "#e7e5e4"
  label: "pkg/cmdguard/v3\n8.2k LOC · 60 files"
}

sub_modules: {
  shape: package
  style.fill: "#18181d"
  style.font-color: "#6eb5ff"
  label: "5 sub-modules\nglamour · manpage\nprompts · spinner\ntelemetry"
}

deps: {
  shape: stored_data
  style.fill: "#18181d"
  style.font-color: "#9ca3af"
  label: "13 direct deps\ncobra · samber/do\nfang · go-output"
}

core -> sub_modules: "extension hooks\nWithHelpTransform · PromptRunner · Middleware"
core -> deps: "depends on"

delta_a: {
  shape: callout
  style.fill: "#fbbf24"
  style.font-color: "#0c0a09"
  label: "Delta A (v4, optional)\nSplit v3 into v3 + v3/internal\nTrigger: LOC > 12k\nCurrent: 8.2k — NO ACTION"
}

delta_b: {
  shape: callout
  style.fill: "#fbbf24"
  style.font-color: "#0c0a09"
  label: "Delta B (v4, optional)\nTypeHandler to TypeCodec\nTrigger: v4 major\nStatus: TODO(v4) added"
}

delta_a -> core: "gates on LOC"
delta_b -> core: "gates on v4"

verdict: {
  shape: text
  style.font-color: "#4ade80"
  label: "Current architecture IS the target.\nBoth deltas are optional refinements.\nDo nothing unless a driver appears."
}
